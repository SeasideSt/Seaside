#summary Release Early. Release Often

 # Load all the latest code from `http://www.squeaksource.com/Seaside30/` and `http://www.squeaksource.com/Seaside30LGPL`.
   # Run the unit tests and make sure they all pass. 
   # Click through the functional test suite using the all server adaptors and all different encodings.
 # Load `Package-Dependencies` from `http://source.lukas-renggli.ch/packaging`.
  # You will need GraphViz packages from there too
  # Generate the graph `PDPackageAnalizer saveSeasideDependencyGraph` and make sure the dependencies are valid.
  # Export as SVG and PNG versions and include them with the release.
 # Update PastReleases.
 # Prepare a list of resolved issues: http://code.google.com/p/seaside/issues/csv?can=1&q=&colspec=ID+Type+Status+Priority+Version+Platform+Owner+Summary+Milestone+Stars+Opened+Closed+Modified+BlockedOn+Blocking+Blocked+MergedInto+Reporter+Cc+Release&cells=tiles
 # Announce the release in the mailing list.
 # Repeat forever.